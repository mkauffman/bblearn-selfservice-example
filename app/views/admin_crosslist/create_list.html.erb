<div class="contnt">
	<b>Create a xlist with section <%= params[:sect_id] %> as the master child.</b>
    <br/>
    You have chosen to create a cross-list of sections. This means that all the sections you
    <br/>
    choose will all have the same content.
    <br/>
	<br/>
	Please the sections you want to crosslist with <%= params[:sect_id] %>.
	<br/>
	<% form_tag({:action => "create_name"}) do %>
	<%= hidden_field_tag "par_id", params[:sect_id] %>
		<table class="xlist_list">
			<tr>
				<th id="check_th" class="sorttable_nosort">
            		<%= check_box_tag('check_all', 'check_all', false, :onClick =>
                "$$('input.check').each(function(box){
      box.checked=(box.checked==false)?true:false});") %>
	  			</th>
				<th id="sect_id" class="name">Section ID</th>
			</tr>
			<% for sect in @sections %>
				<% unless (sect.section_id == params[:sect_id]) or ((/xlist-.*/).match(sect.section_id)) %>
				<tr>
					<td>
						<%= check_box_tag 'sect_id[]', sect.section_id, false, :class => "check" %>
					</td>
					<td>
						<%= sect.section_id %>
					</td>
				</tr>
				<% end # unless sect.id = :sect_id %>
			<% end # for sect in @sections %>
		</table>
		<%= submit_tag "Submit" %>
		<%= submit_tag "Cancel" %>
    <% end %>
</div>