<div class="contnt">
    <% @sections.each do |s| %>
			<% link_url = url_for(:controller => "course_role", :action => "edit_designers",
				:id => s.pk1) %>
            <tr>
                </td>
                <td onClick="window.location='<%= link_url %>'">
                    <%= s.course_id %>
                </td>
                <td onClick="window.location='<%= link_url %>'">
                    <%= s.start_date %>
                </td>
                <td onClick="window.location='<%= link_url %>'">
                    <%= s.end_date %>
                </td>
        <td>
      <% end %>
</div>

